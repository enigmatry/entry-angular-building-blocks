/** Options shared by the file input's validation rules. */
export interface FileValidatorConfig {
  /** A user-facing message to report with the error, in place of the default, which carries none. */
  message?: string;
}
